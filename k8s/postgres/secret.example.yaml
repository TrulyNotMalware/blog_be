# Template only — DO NOT commit a populated copy.
# POSTGRES_PASSWORD must match the password inside blog-app-secret.DATABASE_URL.
apiVersion: v1
kind: Secret
metadata:
  name: blog-postgres-secret
  namespace: blog
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: blog
type: Opaque
stringData:
  POSTGRES_USER: "blog"
  POSTGRES_PASSWORD: "REPLACE_ME_password"
  POSTGRES_DB: "blog"
